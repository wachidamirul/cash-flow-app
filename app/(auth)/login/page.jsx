import LoginPageClient from "./pageClient";

export const metadata = {
  title: "Cash Flow | Login",
  description: "A simple cash flow app",
};

const LoginPage = () => {
  return <LoginPageClient />;
};

export default LoginPage;
