import PageClient from "./pageClient";

export const metadata = {
	title: "Cash Flow",
	description: "A simple cash flow app"
};

const Home = () => {
	return <PageClient />;
};

export default Home;
